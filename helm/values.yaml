# k8s.namespace, image.tag e ops.env definidos via jenkins

app:
  name: spa
  health:
    path: /
    
k8s:
  namespace:
  
image:
  repository: deltacare/spa
  tag:
  
service:
  type: ClusterIP
  port: 80
  
ops:
  env:
